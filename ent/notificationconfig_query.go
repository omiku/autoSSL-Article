// Code generated by ent, DO NOT EDIT.

package ent

import (
	"autoSSL/ent/notificationconfig"
	"autoSSL/ent/predicate"
	"autoSSL/ent/user"
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// NotificationConfigQuery is the builder for querying NotificationConfig entities.
type NotificationConfigQuery struct {
	config
	ctx        *QueryContext
	order      []notificationconfig.OrderOption
	inters     []Interceptor
	predicates []predicate.NotificationConfig
	withUser   *UserQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NotificationConfigQuery builder.
func (ncq *NotificationConfigQuery) Where(ps ...predicate.NotificationConfig) *NotificationConfigQuery {
	ncq.predicates = append(ncq.predicates, ps...)
	return ncq
}

// Limit the number of records to be returned by this query.
func (ncq *NotificationConfigQuery) Limit(limit int) *NotificationConfigQuery {
	ncq.ctx.Limit = &limit
	return ncq
}

// Offset to start from.
func (ncq *NotificationConfigQuery) Offset(offset int) *NotificationConfigQuery {
	ncq.ctx.Offset = &offset
	return ncq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ncq *NotificationConfigQuery) Unique(unique bool) *NotificationConfigQuery {
	ncq.ctx.Unique = &unique
	return ncq
}

// Order specifies how the records should be ordered.
func (ncq *NotificationConfigQuery) Order(o ...notificationconfig.OrderOption) *NotificationConfigQuery {
	ncq.order = append(ncq.order, o...)
	return ncq
}

// QueryUser chains the current query on the "user" edge.
func (ncq *NotificationConfigQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: ncq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ncq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ncq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(notificationconfig.Table, notificationconfig.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, notificationconfig.UserTable, notificationconfig.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(ncq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first NotificationConfig entity from the query.
// Returns a *NotFoundError when no NotificationConfig was found.
func (ncq *NotificationConfigQuery) First(ctx context.Context) (*NotificationConfig, error) {
	nodes, err := ncq.Limit(1).All(setContextOp(ctx, ncq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{notificationconfig.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ncq *NotificationConfigQuery) FirstX(ctx context.Context) *NotificationConfig {
	node, err := ncq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NotificationConfig ID from the query.
// Returns a *NotFoundError when no NotificationConfig ID was found.
func (ncq *NotificationConfigQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ncq.Limit(1).IDs(setContextOp(ctx, ncq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{notificationconfig.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ncq *NotificationConfigQuery) FirstIDX(ctx context.Context) int {
	id, err := ncq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NotificationConfig entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NotificationConfig entity is found.
// Returns a *NotFoundError when no NotificationConfig entities are found.
func (ncq *NotificationConfigQuery) Only(ctx context.Context) (*NotificationConfig, error) {
	nodes, err := ncq.Limit(2).All(setContextOp(ctx, ncq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{notificationconfig.Label}
	default:
		return nil, &NotSingularError{notificationconfig.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ncq *NotificationConfigQuery) OnlyX(ctx context.Context) *NotificationConfig {
	node, err := ncq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NotificationConfig ID in the query.
// Returns a *NotSingularError when more than one NotificationConfig ID is found.
// Returns a *NotFoundError when no entities are found.
func (ncq *NotificationConfigQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ncq.Limit(2).IDs(setContextOp(ctx, ncq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{notificationconfig.Label}
	default:
		err = &NotSingularError{notificationconfig.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ncq *NotificationConfigQuery) OnlyIDX(ctx context.Context) int {
	id, err := ncq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NotificationConfigs.
func (ncq *NotificationConfigQuery) All(ctx context.Context) ([]*NotificationConfig, error) {
	ctx = setContextOp(ctx, ncq.ctx, ent.OpQueryAll)
	if err := ncq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*NotificationConfig, *NotificationConfigQuery]()
	return withInterceptors[[]*NotificationConfig](ctx, ncq, qr, ncq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ncq *NotificationConfigQuery) AllX(ctx context.Context) []*NotificationConfig {
	nodes, err := ncq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NotificationConfig IDs.
func (ncq *NotificationConfigQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ncq.ctx.Unique == nil && ncq.path != nil {
		ncq.Unique(true)
	}
	ctx = setContextOp(ctx, ncq.ctx, ent.OpQueryIDs)
	if err = ncq.Select(notificationconfig.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ncq *NotificationConfigQuery) IDsX(ctx context.Context) []int {
	ids, err := ncq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ncq *NotificationConfigQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ncq.ctx, ent.OpQueryCount)
	if err := ncq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ncq, querierCount[*NotificationConfigQuery](), ncq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ncq *NotificationConfigQuery) CountX(ctx context.Context) int {
	count, err := ncq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ncq *NotificationConfigQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ncq.ctx, ent.OpQueryExist)
	switch _, err := ncq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ncq *NotificationConfigQuery) ExistX(ctx context.Context) bool {
	exist, err := ncq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NotificationConfigQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ncq *NotificationConfigQuery) Clone() *NotificationConfigQuery {
	if ncq == nil {
		return nil
	}
	return &NotificationConfigQuery{
		config:     ncq.config,
		ctx:        ncq.ctx.Clone(),
		order:      append([]notificationconfig.OrderOption{}, ncq.order...),
		inters:     append([]Interceptor{}, ncq.inters...),
		predicates: append([]predicate.NotificationConfig{}, ncq.predicates...),
		withUser:   ncq.withUser.Clone(),
		// clone intermediate query.
		sql:  ncq.sql.Clone(),
		path: ncq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (ncq *NotificationConfigQuery) WithUser(opts ...func(*UserQuery)) *NotificationConfigQuery {
	query := (&UserClient{config: ncq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ncq.withUser = query
	return ncq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.NotificationConfig.Query().
//		GroupBy(notificationconfig.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ncq *NotificationConfigQuery) GroupBy(field string, fields ...string) *NotificationConfigGroupBy {
	ncq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &NotificationConfigGroupBy{build: ncq}
	grbuild.flds = &ncq.ctx.Fields
	grbuild.label = notificationconfig.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.NotificationConfig.Query().
//		Select(notificationconfig.FieldName).
//		Scan(ctx, &v)
func (ncq *NotificationConfigQuery) Select(fields ...string) *NotificationConfigSelect {
	ncq.ctx.Fields = append(ncq.ctx.Fields, fields...)
	sbuild := &NotificationConfigSelect{NotificationConfigQuery: ncq}
	sbuild.label = notificationconfig.Label
	sbuild.flds, sbuild.scan = &ncq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a NotificationConfigSelect configured with the given aggregations.
func (ncq *NotificationConfigQuery) Aggregate(fns ...AggregateFunc) *NotificationConfigSelect {
	return ncq.Select().Aggregate(fns...)
}

func (ncq *NotificationConfigQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ncq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ncq); err != nil {
				return err
			}
		}
	}
	for _, f := range ncq.ctx.Fields {
		if !notificationconfig.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ncq.path != nil {
		prev, err := ncq.path(ctx)
		if err != nil {
			return err
		}
		ncq.sql = prev
	}
	return nil
}

func (ncq *NotificationConfigQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NotificationConfig, error) {
	var (
		nodes       = []*NotificationConfig{}
		withFKs     = ncq.withFKs
		_spec       = ncq.querySpec()
		loadedTypes = [1]bool{
			ncq.withUser != nil,
		}
	)
	if ncq.withUser != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, notificationconfig.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NotificationConfig).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NotificationConfig{config: ncq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ncq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ncq.withUser; query != nil {
		if err := ncq.loadUser(ctx, query, nodes, nil,
			func(n *NotificationConfig, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ncq *NotificationConfigQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*NotificationConfig, init func(*NotificationConfig), assign func(*NotificationConfig, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*NotificationConfig)
	for i := range nodes {
		if nodes[i].user_notification_configs == nil {
			continue
		}
		fk := *nodes[i].user_notification_configs
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_notification_configs" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ncq *NotificationConfigQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ncq.querySpec()
	_spec.Node.Columns = ncq.ctx.Fields
	if len(ncq.ctx.Fields) > 0 {
		_spec.Unique = ncq.ctx.Unique != nil && *ncq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ncq.driver, _spec)
}

func (ncq *NotificationConfigQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(notificationconfig.Table, notificationconfig.Columns, sqlgraph.NewFieldSpec(notificationconfig.FieldID, field.TypeInt))
	_spec.From = ncq.sql
	if unique := ncq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ncq.path != nil {
		_spec.Unique = true
	}
	if fields := ncq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, notificationconfig.FieldID)
		for i := range fields {
			if fields[i] != notificationconfig.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ncq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ncq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ncq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ncq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ncq *NotificationConfigQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ncq.driver.Dialect())
	t1 := builder.Table(notificationconfig.Table)
	columns := ncq.ctx.Fields
	if len(columns) == 0 {
		columns = notificationconfig.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ncq.sql != nil {
		selector = ncq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ncq.ctx.Unique != nil && *ncq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ncq.predicates {
		p(selector)
	}
	for _, p := range ncq.order {
		p(selector)
	}
	if offset := ncq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ncq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// NotificationConfigGroupBy is the group-by builder for NotificationConfig entities.
type NotificationConfigGroupBy struct {
	selector
	build *NotificationConfigQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ncgb *NotificationConfigGroupBy) Aggregate(fns ...AggregateFunc) *NotificationConfigGroupBy {
	ncgb.fns = append(ncgb.fns, fns...)
	return ncgb
}

// Scan applies the selector query and scans the result into the given value.
func (ncgb *NotificationConfigGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ncgb.build.ctx, ent.OpQueryGroupBy)
	if err := ncgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NotificationConfigQuery, *NotificationConfigGroupBy](ctx, ncgb.build, ncgb, ncgb.build.inters, v)
}

func (ncgb *NotificationConfigGroupBy) sqlScan(ctx context.Context, root *NotificationConfigQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ncgb.fns))
	for _, fn := range ncgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ncgb.flds)+len(ncgb.fns))
		for _, f := range *ncgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ncgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ncgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// NotificationConfigSelect is the builder for selecting fields of NotificationConfig entities.
type NotificationConfigSelect struct {
	*NotificationConfigQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ncs *NotificationConfigSelect) Aggregate(fns ...AggregateFunc) *NotificationConfigSelect {
	ncs.fns = append(ncs.fns, fns...)
	return ncs
}

// Scan applies the selector query and scans the result into the given value.
func (ncs *NotificationConfigSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ncs.ctx, ent.OpQuerySelect)
	if err := ncs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NotificationConfigQuery, *NotificationConfigSelect](ctx, ncs.NotificationConfigQuery, ncs, ncs.inters, v)
}

func (ncs *NotificationConfigSelect) sqlScan(ctx context.Context, root *NotificationConfigQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ncs.fns))
	for _, fn := range ncs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ncs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ncs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
