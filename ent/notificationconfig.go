// Code generated by ent, DO NOT EDIT.

package ent

import (
	"autoSSL/ent/notificationconfig"
	"autoSSL/ent/user"
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// NotificationConfig is the model entity for the NotificationConfig schema.
type NotificationConfig struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 配置名称
	Name string `json:"name,omitempty"`
	// 通知类型
	Type notificationconfig.Type `json:"type,omitempty"`
	// 通知配置参数(JSON格式)
	// 				- webhook: {url:string, secret:string}
	// 				- email: {smtp_server:string, smtp_port:int, username:string, password:string, from:string}
	// 				- sms: {api_key:string, template_id:string, sign_name:string}
	Config map[string]interface{} `json:"config,omitempty"`
	// 是否启用
	IsActive bool `json:"is_active,omitempty"`
	// 创建时间
	CreatedAt time.Time `json:"created_at,omitempty"`
	// 更新时间
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the NotificationConfigQuery when eager-loading is set.
	Edges                     NotificationConfigEdges `json:"edges"`
	user_notification_configs *int
	selectValues              sql.SelectValues
}

// NotificationConfigEdges holds the relations/edges for other nodes in the graph.
type NotificationConfigEdges struct {
	// 所属用户
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e NotificationConfigEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*NotificationConfig) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case notificationconfig.FieldConfig:
			values[i] = new([]byte)
		case notificationconfig.FieldIsActive:
			values[i] = new(sql.NullBool)
		case notificationconfig.FieldID:
			values[i] = new(sql.NullInt64)
		case notificationconfig.FieldName, notificationconfig.FieldType:
			values[i] = new(sql.NullString)
		case notificationconfig.FieldCreatedAt, notificationconfig.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case notificationconfig.ForeignKeys[0]: // user_notification_configs
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the NotificationConfig fields.
func (nc *NotificationConfig) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case notificationconfig.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			nc.ID = int(value.Int64)
		case notificationconfig.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				nc.Name = value.String
			}
		case notificationconfig.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				nc.Type = notificationconfig.Type(value.String)
			}
		case notificationconfig.FieldConfig:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field config", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &nc.Config); err != nil {
					return fmt.Errorf("unmarshal field config: %w", err)
				}
			}
		case notificationconfig.FieldIsActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_active", values[i])
			} else if value.Valid {
				nc.IsActive = value.Bool
			}
		case notificationconfig.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				nc.CreatedAt = value.Time
			}
		case notificationconfig.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				nc.UpdatedAt = value.Time
			}
		case notificationconfig.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_notification_configs", value)
			} else if value.Valid {
				nc.user_notification_configs = new(int)
				*nc.user_notification_configs = int(value.Int64)
			}
		default:
			nc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the NotificationConfig.
// This includes values selected through modifiers, order, etc.
func (nc *NotificationConfig) Value(name string) (ent.Value, error) {
	return nc.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the NotificationConfig entity.
func (nc *NotificationConfig) QueryUser() *UserQuery {
	return NewNotificationConfigClient(nc.config).QueryUser(nc)
}

// Update returns a builder for updating this NotificationConfig.
// Note that you need to call NotificationConfig.Unwrap() before calling this method if this NotificationConfig
// was returned from a transaction, and the transaction was committed or rolled back.
func (nc *NotificationConfig) Update() *NotificationConfigUpdateOne {
	return NewNotificationConfigClient(nc.config).UpdateOne(nc)
}

// Unwrap unwraps the NotificationConfig entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (nc *NotificationConfig) Unwrap() *NotificationConfig {
	_tx, ok := nc.config.driver.(*txDriver)
	if !ok {
		panic("ent: NotificationConfig is not a transactional entity")
	}
	nc.config.driver = _tx.drv
	return nc
}

// String implements the fmt.Stringer.
func (nc *NotificationConfig) String() string {
	var builder strings.Builder
	builder.WriteString("NotificationConfig(")
	builder.WriteString(fmt.Sprintf("id=%v, ", nc.ID))
	builder.WriteString("name=")
	builder.WriteString(nc.Name)
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", nc.Type))
	builder.WriteString(", ")
	builder.WriteString("config=")
	builder.WriteString(fmt.Sprintf("%v", nc.Config))
	builder.WriteString(", ")
	builder.WriteString("is_active=")
	builder.WriteString(fmt.Sprintf("%v", nc.IsActive))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(nc.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(nc.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// NotificationConfigs is a parsable slice of NotificationConfig.
type NotificationConfigs []*NotificationConfig
