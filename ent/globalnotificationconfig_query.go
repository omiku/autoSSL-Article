// Code generated by ent, DO NOT EDIT.

package ent

import (
	"autoSSL/ent/globalnotificationconfig"
	"autoSSL/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// GlobalNotificationConfigQuery is the builder for querying GlobalNotificationConfig entities.
type GlobalNotificationConfigQuery struct {
	config
	ctx        *QueryContext
	order      []globalnotificationconfig.OrderOption
	inters     []Interceptor
	predicates []predicate.GlobalNotificationConfig
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GlobalNotificationConfigQuery builder.
func (gncq *GlobalNotificationConfigQuery) Where(ps ...predicate.GlobalNotificationConfig) *GlobalNotificationConfigQuery {
	gncq.predicates = append(gncq.predicates, ps...)
	return gncq
}

// Limit the number of records to be returned by this query.
func (gncq *GlobalNotificationConfigQuery) Limit(limit int) *GlobalNotificationConfigQuery {
	gncq.ctx.Limit = &limit
	return gncq
}

// Offset to start from.
func (gncq *GlobalNotificationConfigQuery) Offset(offset int) *GlobalNotificationConfigQuery {
	gncq.ctx.Offset = &offset
	return gncq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (gncq *GlobalNotificationConfigQuery) Unique(unique bool) *GlobalNotificationConfigQuery {
	gncq.ctx.Unique = &unique
	return gncq
}

// Order specifies how the records should be ordered.
func (gncq *GlobalNotificationConfigQuery) Order(o ...globalnotificationconfig.OrderOption) *GlobalNotificationConfigQuery {
	gncq.order = append(gncq.order, o...)
	return gncq
}

// First returns the first GlobalNotificationConfig entity from the query.
// Returns a *NotFoundError when no GlobalNotificationConfig was found.
func (gncq *GlobalNotificationConfigQuery) First(ctx context.Context) (*GlobalNotificationConfig, error) {
	nodes, err := gncq.Limit(1).All(setContextOp(ctx, gncq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{globalnotificationconfig.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (gncq *GlobalNotificationConfigQuery) FirstX(ctx context.Context) *GlobalNotificationConfig {
	node, err := gncq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GlobalNotificationConfig ID from the query.
// Returns a *NotFoundError when no GlobalNotificationConfig ID was found.
func (gncq *GlobalNotificationConfigQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = gncq.Limit(1).IDs(setContextOp(ctx, gncq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{globalnotificationconfig.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (gncq *GlobalNotificationConfigQuery) FirstIDX(ctx context.Context) int {
	id, err := gncq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GlobalNotificationConfig entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GlobalNotificationConfig entity is found.
// Returns a *NotFoundError when no GlobalNotificationConfig entities are found.
func (gncq *GlobalNotificationConfigQuery) Only(ctx context.Context) (*GlobalNotificationConfig, error) {
	nodes, err := gncq.Limit(2).All(setContextOp(ctx, gncq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{globalnotificationconfig.Label}
	default:
		return nil, &NotSingularError{globalnotificationconfig.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (gncq *GlobalNotificationConfigQuery) OnlyX(ctx context.Context) *GlobalNotificationConfig {
	node, err := gncq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GlobalNotificationConfig ID in the query.
// Returns a *NotSingularError when more than one GlobalNotificationConfig ID is found.
// Returns a *NotFoundError when no entities are found.
func (gncq *GlobalNotificationConfigQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = gncq.Limit(2).IDs(setContextOp(ctx, gncq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{globalnotificationconfig.Label}
	default:
		err = &NotSingularError{globalnotificationconfig.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (gncq *GlobalNotificationConfigQuery) OnlyIDX(ctx context.Context) int {
	id, err := gncq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GlobalNotificationConfigs.
func (gncq *GlobalNotificationConfigQuery) All(ctx context.Context) ([]*GlobalNotificationConfig, error) {
	ctx = setContextOp(ctx, gncq.ctx, ent.OpQueryAll)
	if err := gncq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GlobalNotificationConfig, *GlobalNotificationConfigQuery]()
	return withInterceptors[[]*GlobalNotificationConfig](ctx, gncq, qr, gncq.inters)
}

// AllX is like All, but panics if an error occurs.
func (gncq *GlobalNotificationConfigQuery) AllX(ctx context.Context) []*GlobalNotificationConfig {
	nodes, err := gncq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GlobalNotificationConfig IDs.
func (gncq *GlobalNotificationConfigQuery) IDs(ctx context.Context) (ids []int, err error) {
	if gncq.ctx.Unique == nil && gncq.path != nil {
		gncq.Unique(true)
	}
	ctx = setContextOp(ctx, gncq.ctx, ent.OpQueryIDs)
	if err = gncq.Select(globalnotificationconfig.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (gncq *GlobalNotificationConfigQuery) IDsX(ctx context.Context) []int {
	ids, err := gncq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (gncq *GlobalNotificationConfigQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, gncq.ctx, ent.OpQueryCount)
	if err := gncq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, gncq, querierCount[*GlobalNotificationConfigQuery](), gncq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (gncq *GlobalNotificationConfigQuery) CountX(ctx context.Context) int {
	count, err := gncq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (gncq *GlobalNotificationConfigQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, gncq.ctx, ent.OpQueryExist)
	switch _, err := gncq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (gncq *GlobalNotificationConfigQuery) ExistX(ctx context.Context) bool {
	exist, err := gncq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GlobalNotificationConfigQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (gncq *GlobalNotificationConfigQuery) Clone() *GlobalNotificationConfigQuery {
	if gncq == nil {
		return nil
	}
	return &GlobalNotificationConfigQuery{
		config:     gncq.config,
		ctx:        gncq.ctx.Clone(),
		order:      append([]globalnotificationconfig.OrderOption{}, gncq.order...),
		inters:     append([]Interceptor{}, gncq.inters...),
		predicates: append([]predicate.GlobalNotificationConfig{}, gncq.predicates...),
		// clone intermediate query.
		sql:  gncq.sql.Clone(),
		path: gncq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GlobalNotificationConfig.Query().
//		GroupBy(globalnotificationconfig.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (gncq *GlobalNotificationConfigQuery) GroupBy(field string, fields ...string) *GlobalNotificationConfigGroupBy {
	gncq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GlobalNotificationConfigGroupBy{build: gncq}
	grbuild.flds = &gncq.ctx.Fields
	grbuild.label = globalnotificationconfig.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.GlobalNotificationConfig.Query().
//		Select(globalnotificationconfig.FieldName).
//		Scan(ctx, &v)
func (gncq *GlobalNotificationConfigQuery) Select(fields ...string) *GlobalNotificationConfigSelect {
	gncq.ctx.Fields = append(gncq.ctx.Fields, fields...)
	sbuild := &GlobalNotificationConfigSelect{GlobalNotificationConfigQuery: gncq}
	sbuild.label = globalnotificationconfig.Label
	sbuild.flds, sbuild.scan = &gncq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GlobalNotificationConfigSelect configured with the given aggregations.
func (gncq *GlobalNotificationConfigQuery) Aggregate(fns ...AggregateFunc) *GlobalNotificationConfigSelect {
	return gncq.Select().Aggregate(fns...)
}

func (gncq *GlobalNotificationConfigQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range gncq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, gncq); err != nil {
				return err
			}
		}
	}
	for _, f := range gncq.ctx.Fields {
		if !globalnotificationconfig.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if gncq.path != nil {
		prev, err := gncq.path(ctx)
		if err != nil {
			return err
		}
		gncq.sql = prev
	}
	return nil
}

func (gncq *GlobalNotificationConfigQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GlobalNotificationConfig, error) {
	var (
		nodes = []*GlobalNotificationConfig{}
		_spec = gncq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GlobalNotificationConfig).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GlobalNotificationConfig{config: gncq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, gncq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (gncq *GlobalNotificationConfigQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := gncq.querySpec()
	_spec.Node.Columns = gncq.ctx.Fields
	if len(gncq.ctx.Fields) > 0 {
		_spec.Unique = gncq.ctx.Unique != nil && *gncq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, gncq.driver, _spec)
}

func (gncq *GlobalNotificationConfigQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(globalnotificationconfig.Table, globalnotificationconfig.Columns, sqlgraph.NewFieldSpec(globalnotificationconfig.FieldID, field.TypeInt))
	_spec.From = gncq.sql
	if unique := gncq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if gncq.path != nil {
		_spec.Unique = true
	}
	if fields := gncq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, globalnotificationconfig.FieldID)
		for i := range fields {
			if fields[i] != globalnotificationconfig.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := gncq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := gncq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := gncq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := gncq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (gncq *GlobalNotificationConfigQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(gncq.driver.Dialect())
	t1 := builder.Table(globalnotificationconfig.Table)
	columns := gncq.ctx.Fields
	if len(columns) == 0 {
		columns = globalnotificationconfig.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if gncq.sql != nil {
		selector = gncq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if gncq.ctx.Unique != nil && *gncq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range gncq.predicates {
		p(selector)
	}
	for _, p := range gncq.order {
		p(selector)
	}
	if offset := gncq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := gncq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// GlobalNotificationConfigGroupBy is the group-by builder for GlobalNotificationConfig entities.
type GlobalNotificationConfigGroupBy struct {
	selector
	build *GlobalNotificationConfigQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gncgb *GlobalNotificationConfigGroupBy) Aggregate(fns ...AggregateFunc) *GlobalNotificationConfigGroupBy {
	gncgb.fns = append(gncgb.fns, fns...)
	return gncgb
}

// Scan applies the selector query and scans the result into the given value.
func (gncgb *GlobalNotificationConfigGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gncgb.build.ctx, ent.OpQueryGroupBy)
	if err := gncgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GlobalNotificationConfigQuery, *GlobalNotificationConfigGroupBy](ctx, gncgb.build, gncgb, gncgb.build.inters, v)
}

func (gncgb *GlobalNotificationConfigGroupBy) sqlScan(ctx context.Context, root *GlobalNotificationConfigQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(gncgb.fns))
	for _, fn := range gncgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*gncgb.flds)+len(gncgb.fns))
		for _, f := range *gncgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*gncgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gncgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GlobalNotificationConfigSelect is the builder for selecting fields of GlobalNotificationConfig entities.
type GlobalNotificationConfigSelect struct {
	*GlobalNotificationConfigQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (gncs *GlobalNotificationConfigSelect) Aggregate(fns ...AggregateFunc) *GlobalNotificationConfigSelect {
	gncs.fns = append(gncs.fns, fns...)
	return gncs
}

// Scan applies the selector query and scans the result into the given value.
func (gncs *GlobalNotificationConfigSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gncs.ctx, ent.OpQuerySelect)
	if err := gncs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GlobalNotificationConfigQuery, *GlobalNotificationConfigSelect](ctx, gncs.GlobalNotificationConfigQuery, gncs, gncs.inters, v)
}

func (gncs *GlobalNotificationConfigSelect) sqlScan(ctx context.Context, root *GlobalNotificationConfigQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(gncs.fns))
	for _, fn := range gncs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*gncs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gncs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
