// Code generated by ent, DO NOT EDIT.

package ent

import (
	"autoSSL/ent/certificate"
	"autoSSL/ent/workflow"
	"autoSSL/ent/workflowstep"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// WorkflowStep is the model entity for the WorkflowStep schema.
type WorkflowStep struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 步骤名称
	Name string `json:"name,omitempty"`
	// 步骤状态
	Status workflowstep.Status `json:"status,omitempty"`
	// 步骤编号
	StepNumber int `json:"step_number,omitempty"`
	// 步骤日志
	Log string `json:"log,omitempty"`
	// 开始时间
	StartedAt time.Time `json:"started_at,omitempty"`
	// 完成时间
	CompletedAt time.Time `json:"completed_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the WorkflowStepQuery when eager-loading is set.
	Edges                      WorkflowStepEdges `json:"edges"`
	certificate_workflow_steps *int
	workflow_steps             *int
	selectValues               sql.SelectValues
}

// WorkflowStepEdges holds the relations/edges for other nodes in the graph.
type WorkflowStepEdges struct {
	// Certificate holds the value of the certificate edge.
	Certificate *Certificate `json:"certificate,omitempty"`
	// 所属用户
	User []*User `json:"user,omitempty"`
	// 所属工作流
	Workflow *Workflow `json:"workflow,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// CertificateOrErr returns the Certificate value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e WorkflowStepEdges) CertificateOrErr() (*Certificate, error) {
	if e.Certificate != nil {
		return e.Certificate, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: certificate.Label}
	}
	return nil, &NotLoadedError{edge: "certificate"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading.
func (e WorkflowStepEdges) UserOrErr() ([]*User, error) {
	if e.loadedTypes[1] {
		return e.User, nil
	}
	return nil, &NotLoadedError{edge: "user"}
}

// WorkflowOrErr returns the Workflow value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e WorkflowStepEdges) WorkflowOrErr() (*Workflow, error) {
	if e.Workflow != nil {
		return e.Workflow, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: workflow.Label}
	}
	return nil, &NotLoadedError{edge: "workflow"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*WorkflowStep) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case workflowstep.FieldID, workflowstep.FieldStepNumber:
			values[i] = new(sql.NullInt64)
		case workflowstep.FieldName, workflowstep.FieldStatus, workflowstep.FieldLog:
			values[i] = new(sql.NullString)
		case workflowstep.FieldStartedAt, workflowstep.FieldCompletedAt, workflowstep.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case workflowstep.ForeignKeys[0]: // certificate_workflow_steps
			values[i] = new(sql.NullInt64)
		case workflowstep.ForeignKeys[1]: // workflow_steps
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the WorkflowStep fields.
func (ws *WorkflowStep) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case workflowstep.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ws.ID = int(value.Int64)
		case workflowstep.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				ws.Name = value.String
			}
		case workflowstep.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				ws.Status = workflowstep.Status(value.String)
			}
		case workflowstep.FieldStepNumber:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field step_number", values[i])
			} else if value.Valid {
				ws.StepNumber = int(value.Int64)
			}
		case workflowstep.FieldLog:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field log", values[i])
			} else if value.Valid {
				ws.Log = value.String
			}
		case workflowstep.FieldStartedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field started_at", values[i])
			} else if value.Valid {
				ws.StartedAt = value.Time
			}
		case workflowstep.FieldCompletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field completed_at", values[i])
			} else if value.Valid {
				ws.CompletedAt = value.Time
			}
		case workflowstep.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ws.CreatedAt = value.Time
			}
		case workflowstep.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field certificate_workflow_steps", value)
			} else if value.Valid {
				ws.certificate_workflow_steps = new(int)
				*ws.certificate_workflow_steps = int(value.Int64)
			}
		case workflowstep.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field workflow_steps", value)
			} else if value.Valid {
				ws.workflow_steps = new(int)
				*ws.workflow_steps = int(value.Int64)
			}
		default:
			ws.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the WorkflowStep.
// This includes values selected through modifiers, order, etc.
func (ws *WorkflowStep) Value(name string) (ent.Value, error) {
	return ws.selectValues.Get(name)
}

// QueryCertificate queries the "certificate" edge of the WorkflowStep entity.
func (ws *WorkflowStep) QueryCertificate() *CertificateQuery {
	return NewWorkflowStepClient(ws.config).QueryCertificate(ws)
}

// QueryUser queries the "user" edge of the WorkflowStep entity.
func (ws *WorkflowStep) QueryUser() *UserQuery {
	return NewWorkflowStepClient(ws.config).QueryUser(ws)
}

// QueryWorkflow queries the "workflow" edge of the WorkflowStep entity.
func (ws *WorkflowStep) QueryWorkflow() *WorkflowQuery {
	return NewWorkflowStepClient(ws.config).QueryWorkflow(ws)
}

// Update returns a builder for updating this WorkflowStep.
// Note that you need to call WorkflowStep.Unwrap() before calling this method if this WorkflowStep
// was returned from a transaction, and the transaction was committed or rolled back.
func (ws *WorkflowStep) Update() *WorkflowStepUpdateOne {
	return NewWorkflowStepClient(ws.config).UpdateOne(ws)
}

// Unwrap unwraps the WorkflowStep entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ws *WorkflowStep) Unwrap() *WorkflowStep {
	_tx, ok := ws.config.driver.(*txDriver)
	if !ok {
		panic("ent: WorkflowStep is not a transactional entity")
	}
	ws.config.driver = _tx.drv
	return ws
}

// String implements the fmt.Stringer.
func (ws *WorkflowStep) String() string {
	var builder strings.Builder
	builder.WriteString("WorkflowStep(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ws.ID))
	builder.WriteString("name=")
	builder.WriteString(ws.Name)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", ws.Status))
	builder.WriteString(", ")
	builder.WriteString("step_number=")
	builder.WriteString(fmt.Sprintf("%v", ws.StepNumber))
	builder.WriteString(", ")
	builder.WriteString("log=")
	builder.WriteString(ws.Log)
	builder.WriteString(", ")
	builder.WriteString("started_at=")
	builder.WriteString(ws.StartedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("completed_at=")
	builder.WriteString(ws.CompletedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ws.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// WorkflowSteps is a parsable slice of WorkflowStep.
type WorkflowSteps []*WorkflowStep
